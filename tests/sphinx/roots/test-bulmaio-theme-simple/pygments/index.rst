=============
Test Pygments
=============

Let's see if Pygments styling looks ok.

.. code-block:: python
    :linenos:
    :emphasize-lines: 2-3

    def foo(bar):
        msg = 'Hello ' + bar
        return msg

    foo('bar')

And now after.